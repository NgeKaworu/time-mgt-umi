import React, { useEffect } from "react";
import { Button, Input, message } from "antd";

export default () => {
  useEffect(() => {
    console.log("123");
  });
  return (
    <div>
      <h1>Page index</h1>
    </div>
  );
};
